apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "claims-ui.fullname" . }}-test-connection"
  labels:
    {{- include "claims-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "claims-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
